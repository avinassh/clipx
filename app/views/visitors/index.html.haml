%h2
  ClipX
- if current_user
  %div
    Pocket Integration :
    - if current_user.pocket_account
      %span
        = current_user.pocket_account.username
        Last fetched
        = time_ago_in_words Time.at(current_user.pocket_account.last_fetched).to_datetime
        ago
        = link_to "(Re-integrate)", user_omniauth_authorize_path(:pocket)
    - else
      = link_to "Integrate with pocket", user_omniauth_authorize_path(:pocket)
  %div
    Twitter Integration :
    - if current_user.twitter_account
      %span
        = current_user.twitter_account.username
        Last fetched
        - if current_user.twitter_account.last_fetched.nil?
          Never
        - else
          = time_ago_in_words Time.at(current_user.twitter_account.last_fetched).to_datetime
        ago
        = link_to "(Re-integrate)", user_omniauth_authorize_path(:twitter)
    - else
      = link_to "Integrate with twitter", user_omniauth_authorize_path(:twitter)
  %div
    Evernote Integration :
    - if current_user.evernote_account
      %span
        = current_user.evernote_account.username
        Last published
        = time_ago_in_words Time.at(current_user.evernote_account.last_published).to_datetime
        ago
        = link_to "(Re-integrate)", user_omniauth_authorize_path(:evernote)
    - else
      = link_to "Integrate with evernote", user_omniauth_authorize_path(:evernote)
  %div
    Google Drive Integration :
    - if current_user.google_account
      %span
        = current_user.google_account.email
        Last published
        = time_ago_in_words Time.at(current_user.google_account.last_published).to_datetime
        ago
        = link_to "(Re-integrate)", user_omniauth_authorize_path(:google)
    - else
      = link_to "Integrate with google", user_omniauth_authorize_path(:google)
  %div
    Github Integration :
    - if current_user.github_account
      %span
        = current_user.github_account.username
        Last fetched
        - if current_user.github_account.last_fetched.nil?
          Never
        - else
          = time_ago_in_words Time.at(current_user.github_account.last_fetched).to_datetime
        ago
        = link_to "(Re-integrate)", user_omniauth_authorize_path(:github)
    - else
      = link_to "Integrate with GitHub", user_omniauth_authorize_path(:github)
  %div
    Dropbox Integration :
    - if current_user.dropbox_account
      %span
        = current_user.dropbox_account.email
        = link_to "(Re-integrate)", user_omniauth_authorize_path(:dropbox)
    - else
      = link_to "Integrate with Dropbox", user_omniauth_authorize_path(:dropbox)

- else
  You are not logged in