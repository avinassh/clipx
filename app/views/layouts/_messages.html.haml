-# Rails flash messages styled for Bootstrap 3.0
- flash.each do |name, msg|
  - if msg.is_a?(String)
    %div.flash
      %span{:class => "flash-#{name.to_s == 'notice' ? 'success' : 'danger'}"}
        = msg
        %button.no-bg.close{:type => 'button'} ×